# SAE J1939 Protokol Referansı (STM32 / FDCAN)

<!-- @trust-header v1 -->
> **Trust level for this reference**
>
> - **Design patterns, decision trees, errata workarounds, protocol-spec content** here is authoritative — that is why this file exists.
> - **Inline HAL/CMSIS/peripheral code snippets** are illustrative. The HAL drifts between versions and parts. For the canonical version of any HAL symbol at your HAL release: `gh search code <SymbolName> --owner=STMicroelectronics --extension=c` — see [ref-st-github-map.md](ref-st-github-map.md) §8 for the full lookup procedure.
> - **CRITICAL bugs identified in the 2026-05-16 audit have been corrected** in this file, but verify against your own HAL version before copy-pasting.
> - **For bootloader / IAP / OTA topics** the canonical checklist + ARM KA001193 + AN5188/2606/3155/3156 references are in [ref-bootloader.md](ref-bootloader.md).


SAE J1939: ağır araç (kamyon, otobüs, iş makinesi) CAN ağ standardı. 29-bit extended CAN ID, 8 byte veri, 250 kbps (bazı uyg. 500 kbps). ISO 11898-2 fiziksel katman.

---

## 29-bit CAN ID Yapısı

```
Bit[28:26] Priority         3 bit — 0 (en yüksek) … 7 (en düşük)
Bit[25]    Reserved (R)     1 bit — 0
Bit[24]    Data Page (DP)   1 bit — 0 veya 1 (PGN alanını genişletir)
Bit[23:16] PDU Format (PF)  8 bit — mesaj tipi belirler
Bit[15:8]  PDU Specific     8 bit — PF<0xF0: hedef adres; PF≥0xF0: grup uzantısı (GE)
Bit[7:0]   Source Address   8 bit — gönderen node adresi (0x00..0xFD)
                                    0xFE = Null (adres talep ediyor)
                                    0xFF = Broadcast (global adres)
```

**PGN (Parameter Group Number) hesabı:**
```
Eğer PF < 0xF0 (PDU1 — peer-to-peer):
    PGN = (DP << 17) | (PF << 8) | 0x00   ← PDU Specific hedef adres, PGN'e dahil değil

Eğer PF >= 0xF0 (PDU2 — broadcast):
    PGN = (DP << 17) | (PF << 8) | PS    ← PDU Specific grup uzantısı, PGN'e dahil
```

**Örnek CAN ID analizi:**
```
0x0CF00400 (hex) = 0000 1100 1111 0000 0000 0100 0000 0000 (binary)
  Priority = 3     (bits 28:26 = 011)
  R = 0, DP = 0
  PF = 0xF0        → PDU2 (broadcast)
  PS = 0x04        → Group Extension 4
  SA = 0x00        → Engine Control Module #1

PGN = 0x0000F004 = 61444 = EEC1 (Electronic Engine Controller 1)
```

---

## PDU1 vs PDU2

| | PDU1 | PDU2 |
|--|------|------|
| PF range | 0x00..0xEF | 0xF0..0xFF |
| PDU Specific | Hedef adres | Grup uzantısı (GE) |
| Yön | Peer-to-peer (DA belirli) | Broadcast (tüm node'lar) |
| PGN'de PS | Dahil değil | Dahil |
| Örnek | TP.CM (0xEC00), DT (0xEB00) | EEC1 (0xF004), CCVS (0xFEF1) |

---

## Yaygın PGN Tablosu

| PGN (hex) | Adı | Bit Rate | Period |
|-----------|-----|---------|--------|
| 0x00EE00 | Address Claimed | — | Event |
| 0x00EA00 | Request PGN | — | Event |
| 0x00EC00 | TP.CM (Transport Protocol Conn.Mgmt) | — | Event |
| 0x00EB00 | TP.DT (Transport Protocol Data Transfer) | — | Event |
| 0x00FE6B | Cab Illumination | 1 kbps | 1000ms |
| 0x00FEF1 | CCVS (Vehicle Speed) | — | 100ms |
| 0x00FEF2 | LFE (Fuel Economy) | — | 100ms |
| 0x00FEE5 | ET1 (Engine Temp) | — | 1000ms |
| 0x00FECA | DM1 (Active DTCs) | — | 1000ms |
| 0x00FECB | DM2 (Previously Active DTCs) | — | On Request |
| 0x00FEC1 | DM11 (Clear DTCs) | — | On Request |
| 0x00F001 | EEC3 | — | 250ms |
| 0x00F003 | EEC2 | — | 50ms |
| 0x00F004 | EEC1 (Engine Speed/Torque) | — | 10ms |
| 0x00F005 | ETC1 | — | 10ms |

---

## Address Claiming

Her J1939 node ağ katılımında adres talep eder.

```
Adres Talebi (Address Claimed — PGN 0x00EE00):
  CAN ID = [Pri=6, R=0, DP=0, PF=0xEE, PS=0xFF, SA=kendi_adres]
  DATA = 8 byte NAME (node kimliği, manufacturer code, vs)

Eğer başka node aynı adresi talep ederse:
  - Daha düşük NAME (numerik) kazanır
  - Kaybeden node farklı adres seçer veya Null adresle devam eder

Adres sorgusu (Request — PGN 0x00EA00):
  CAN ID = [Pri=6, R=0, DP=0, PF=0xEA, PS=0xFF, SA=0xFE]
  DATA = 00 EE 00 FF FF FF FF FF  (PGN 0xEE00 talep et, FF ile doldur)
```

**NAME yapısı (8 byte, LSB first):**
```
bits[63:60] Industry Group    4 bit (0=Global, 1=Otomasyon, 2=Agri, 3=Const, 4=Marine)
bits[59]    Vehicle System Instance 1 bit
bits[58:51] Vehicle System  8 bit
bits[50]    Reserved         1 bit
bits[49:42] Function         8 bit
bits[41:39] Function Instance 3 bit
bits[38:35] ECU Instance      4 bit
bits[34:11] Manufacturer Code 11 bit
bits[10:0]  Identity Number   21 bit
```

---

## Transport Protocol (TP) — Büyük Mesaj Transferi

J1939-TP: 9 ila 1785 bayt arası mesajlar için. İki mod:

### BAM (Broadcast Announce Message) — Broadcast

```
1. TP.CM_BAM gönder (PGN 0x00ECFF):
   Byte 0:   0x20 (BAM control byte)
   Byte 1-2: Mesaj boyutu (little-endian)
   Byte 3:   Paket sayısı
   Byte 4:   0xFF (reserved)
   Byte 5-7: PGN (little-endian, 3 byte)

2. TP.DT çerçeveleri gönder (PGN 0x00EBFF, 50ms aralıklarla):
   Byte 0:   Sıra numarası (1..255)
   Byte 1-7: Data (7 byte, son paket eksikse 0xFF ile doldur)
```

**Örnek — 20 byte BAM:**
```
TP.CM_BAM: 20 14 00 03 FF 18 F0 00   → 20byte, 3 paket, PGN=0x00F018
TP.DT:     01 D1 D2 D3 D4 D5 D6 D7  → paket 1
TP.DT:     02 D8 D9 DA DB DC DD DE  → paket 2
TP.DT:     03 DF FF FF FF FF FF FF  → paket 3 (son 1 byte + 6 byte pad)
```

### CMDT (Connection Mode Data Transfer) — Peer-to-Peer

```
1. TP.CM_RTS → Alıcıya RTS gönder (PGN 0x00EC + DA):
   Byte 0: 0x10 (RTS control byte)
   Byte 1-2: Mesaj boyutu
   Byte 3: Paket sayısı
   Byte 4: Max paket/CTS (0xFF=no limit)
   Byte 5-7: PGN

2. Alıcı TP.CM_CTS ile onaylar (Byte 0=0x11, byte 1=kabul edilen paket sayısı)

3. Gönderici TP.DT gönderir

4. Alıcı EOM onaylar (Byte 0=0x13)

Hata: 0xFF = Connection Abort (Byte 0=0xFF, Byte 1=hata kodu)
```

---

## DM1 — Aktif DTC Mesajı (PGN 0x00FECA)

```
Byte 0: Lamp status (MIL, Red Stop, Yellow, Protect)
Byte 1: Flash (reserved)
Byte 2-3: DTC (SPN bits[18:11], SPN bits[10:3])
Byte 4:   SPN bits[2:0] + FMI bits[4:0]
Byte 5:   Occurrence count (0..126, 127=unknown)
```

**SPN (Suspect Parameter Number):** Hangi sinyal/sensör  
**FMI (Failure Mode Indicator):** Nasıl arızalandı

| FMI | Açıklama |
|-----|---------|
| 0 | Üst limit aşıldı (>high) |
| 1 | Alt limit aşıldı (<low) |
| 2 | Veri hatalı |
| 3 | Voltaj yüksek / açık devre |
| 4 | Voltaj düşük / kısa devre |
| 5 | Akım düşük |
| 6 | Akım yüksek |
| 7 | Mekanik arıza |
| 8 | Anormal frekans |
| 9 | Anormal güncelleme hızı |
| 10 | Anormal değişim oranı |
| 11 | Kök neden bilinmiyor |
| 12 | Kötü akıllı cihaz |
| 13 | Out of calibration |
| 14 | Özel talimat |
| 31 | Koşul yok |

---

## STM32H7 FDCAN — J1939 Yapılandırması

```c
/* J1939: 29-bit extended ID, 250 kbps (veya 500 kbps) */
FDCAN_HandleTypeDef hfdcan1;

hfdcan1.Init.ClockDivider         = FDCAN_CLOCK_DIV1;   /* HAL macro is _DIV1, not _DIVIDER1 */
hfdcan1.Init.FrameFormat          = FDCAN_FRAME_CLASSIC;   /* CAN 2.0B — J1939 klasik */
hfdcan1.Init.Mode                 = FDCAN_MODE_NORMAL;
hfdcan1.Init.AutoRetransmission   = ENABLE;
hfdcan1.Init.TransmitPause        = ENABLE;   /* J1939 bus access delay */
hfdcan1.Init.ProtocolException    = ENABLE;

/* 250 kbps @ 80 MHz FDCAN clock:
 *   bitrate = clock / (prescaler * (1 + TSEG1 + TSEG2))
 *   250000 = 80_000_000 / (20 * (1 + 13 + 2)) = 80M / 320  ✓
 * Sample point = (1 + TSEG1) / (1 + TSEG1 + TSEG2) = 14/16 = 87.5% (J1939 spec) */
hfdcan1.Init.NominalPrescaler     = 20;
hfdcan1.Init.NominalSyncJumpWidth = 1;
hfdcan1.Init.NominalTimeSeg1      = 13;
hfdcan1.Init.NominalTimeSeg2      = 2;

/* J1939 global filtre: sadece EXT ID kabul et, STD ID reddet */
HAL_FDCAN_ConfigGlobalFilter(&hfdcan1,
    FDCAN_REJECT,           /* non-matching STD frames */
    FDCAN_ACCEPT_IN_RX_FIFO0, /* non-matching EXT frames → FIFO0 */
    FDCAN_FILTER_REMOTE,
    FDCAN_FILTER_REMOTE);

/* Filtre: belirli PGN range kabul — örn. EEC1 + FECA + TP çerçeveleri */
FDCAN_FilterTypeDef f;
f.IdType       = FDCAN_EXTENDED_ID;
f.FilterIndex  = 0;
f.FilterType   = FDCAN_FILTER_MASK;
f.FilterConfig = FDCAN_FILTER_TO_RXFIFO0;
/* PGN 0xF004 (EEC1) — herhangi kaynak adresten */
/* 29-bit mask: priority(3)+R(1)+DP(1)+PF(8)+PS(8)+SA(8) */
/* Sadece PF+PS maskele (PGN kısmı), SA=don't care */
f.FilterID1    = 0x0CF00400;  /* EEC1, SA=0x00 */
f.FilterID2    = 0x03FFFF00;  /* mask: PGN alanını maskele, SA don't care */
HAL_FDCAN_ConfigFilter(&hfdcan1, &f);
```

**29-bit J1939 CAN ID inşası:**
```c
uint32_t j1939_make_id(uint8_t priority, uint32_t pgn, uint8_t src_addr)
{
    /* PDU2: PF >= 0xF0 → PS PGN'in içinde */
    uint8_t pf = (pgn >> 8) & 0xFF;
    uint8_t ps = pgn & 0xFF;
    uint8_t dp = (pgn >> 16) & 0x01;

    return ((uint32_t)(priority & 0x07) << 26)
         | ((uint32_t)(dp)              << 24)
         | ((uint32_t)(pf)              << 16)
         | ((uint32_t)(ps)              <<  8)
         | ((uint32_t)(src_addr));
}

/* EEC1 gönder */
uint32_t can_id = j1939_make_id(3, 0x00F004, MY_SA);
FDCAN_TxHeaderTypeDef hdr = {
    .Identifier          = can_id,
    .IdType              = FDCAN_EXTENDED_ID,
    .TxFrameType         = FDCAN_DATA_FRAME,
    .DataLength          = FDCAN_DLC_BYTES_8,
    .BitRateSwitch       = FDCAN_BRS_OFF,
    .FDFormat            = FDCAN_CLASSIC_CAN,
    .TxEventFifoControl  = FDCAN_NO_TX_EVENTS,
    .MessageMarker       = 0,
};
```

---

## J1939 Adres Yönetimi — STM32 Uygulama

```c
#define J1939_NULL_ADDR    0xFE
#define J1939_GLOBAL_ADDR  0xFF
#define J1939_INITIAL_ADDR 0x80   /* varsayılan talep adresi */

static uint8_t my_address = J1939_NULL_ADDR;
static uint64_t my_name;

void j1939_claim_address(uint8_t desired_addr)
{
    my_address = desired_addr;
    /* Address Claimed mesajı gönder */
    uint32_t can_id = j1939_make_id(6, 0x00EE00, desired_addr);
    uint8_t name_bytes[8];
    memcpy(name_bytes, &my_name, 8);  /* little-endian NAME */
    fdcan_send(can_id, name_bytes, 8);
}

/* Gelen Address Claimed'i kontrol et — çakışma var mı? */
void j1939_rx_address_claimed(uint32_t can_id, const uint8_t *data)
{
    uint8_t sender_sa  = can_id & 0xFF;
    uint64_t sender_name;
    memcpy(&sender_name, data, 8);

    if (sender_sa == my_address) {
        if (sender_name < my_name) {
            /* Kaybettik — yeni adres seç veya null address kullan */
            my_address++;   /* basit: bir sonraki adres dene */
            if (my_address >= 0xFE) my_address = 0x80;
            osDelay(osKernelGetTickCount() % 153);  /* rastgele gecikme (J1939 gereksinimi) */
            j1939_claim_address(my_address);
        } else {
            /* Kazandık — tekrar claim et */
            j1939_claim_address(my_address);
        }
    }
}
```

---

## TP BAM Gönderim — STM32 Uygulama İskeleti

```c
void j1939_send_bam(uint32_t pgn, const uint8_t *data, uint16_t len)
{
    uint8_t num_packets = (uint8_t)((len + 6) / 7);

    /* 1. TP.CM_BAM */
    uint8_t cm[8] = {
        0x20,                      /* BAM */
        len & 0xFF, (len >> 8) & 0xFF,
        num_packets,
        0xFF,
        pgn & 0xFF, (pgn >> 8) & 0xFF, (pgn >> 16) & 0xFF
    };
    fdcan_send(j1939_make_id(7, 0x00ECFF, my_address), cm, 8);

    /* 2. TP.DT — 50ms aralıklarla */
    for (uint8_t pkt = 1; pkt <= num_packets; pkt++) {
        osDelay(50U);
        uint8_t dt[8];
        dt[0] = pkt;
        uint16_t offset = (pkt - 1) * 7;
        for (int i = 0; i < 7; i++) {
            dt[1 + i] = (offset + i < len) ? data[offset + i] : 0xFF;
        }
        fdcan_send(j1939_make_id(7, 0x00EBFF, my_address), dt, 8);
    }
}
```

---

## Yaygın J1939 Hataları

| Hata | Neden | Çözüm |
|------|-------|-------|
| Mesaj alınamıyor | STD ID filtre aktif | `FDCAN_ACCEPT_IN_RX_FIFO0` EXT için |
| PGN yanlış hesaplanıyor | PDU1/PDU2 ayrımı | PF<0xF0: PS adres, PGN'de yok |
| TP timeout | BAM aralarında 50ms yok | `osDelay(50U)` zorunlu |
| Adres çakışması çözülmüyor | NAME karşılaştırması yanlış | NAME little-endian, uint64_t karşılaştır |
| DM1 parse hatası | SPN bit order yanlış | SPN=byte[2-4] karmaşık packing |
| Bus-off | 250 kbps yanlış | prescaler/TSEG kristal frekansına göre hesapla |
| 29-bit ID oluşturulamıyor | STD ID mode seçili | `FDCAN_EXTENDED_ID` kullan |

---

## Address Claiming — Tam State Machine (ISO J1939-81)

ISO J1939-81 tam zamanlama kuralları ile üretim kalitesi AC state machine:

```c
typedef enum {
    AC_IDLE,        /* başlangıç — adres talep edilmemiş */
    AC_WAIT,        /* AC mesajı gönderildi, 250ms bekleniyor */
    AC_CLAIMED,     /* adres başarıyla alındı */
    AC_CANNOT_CLAIM /* tüm adresler meşgul */
} ac_state_t;

static ac_state_t  ac_state    = AC_IDLE;
static uint32_t    ac_timer_ms = 0;

/* J1939-81 §5.4.2: Null address ile Address Claimed gönder */
static void ac_cannot_claim(void)
{
    /* SA=0xFE (Null), Data=kendi NAME — bus'a "adres alamadım" bildir */
    uint32_t id = j1939_make_id(6, 0x00EE00, J1939_NULL_ADDR);
    fdcan_send_ext(id, (uint8_t *)&my_name, 8);
    my_address = J1939_NULL_ADDR;
    ac_state   = AC_CANNOT_CLAIM;
}

static void ac_send_claim(uint8_t addr)
{
    uint32_t id = j1939_make_id(6, 0x00EE00, addr);
    fdcan_send_ext(id, (uint8_t *)&my_name, 8);
    ac_state    = AC_WAIT;
    ac_timer_ms = 0;   /* 250ms pencere başlıyor */
    my_address  = addr;
}

/* Her CAN RX callback'inde çağrılır */
void j1939_ac_rx(uint32_t can_id, const uint8_t *data, uint8_t dlc)
{
    uint32_t pgn    = j1939_pgn_from_id(can_id);
    uint8_t  sender = can_id & 0xFF;

    if (pgn != 0x00EE00 || dlc < 8) return;

    uint64_t sender_name;
    memcpy(&sender_name, data, 8);

    if (sender != my_address) return;  /* farklı adres — ilgilenmiyoruz */

    if (sender_name < my_name) {
        /* Çakışma: karşı taraf kazandı */
        if (ac_state == AC_WAIT || ac_state == AC_CLAIMED) {
            uint8_t next = find_next_preferred_addr();
            if (next == J1939_NULL_ADDR) {
                ac_cannot_claim();
            } else {
                /* 0–153ms rastgele gecikme (J1939-81 §5.4.2) */
                uint32_t delay_ms = (uint32_t)(my_name & 0xFF) % 153U;
                osDelay(delay_ms);
                ac_send_claim(next);
            }
        }
    } else {
        /* Biz kazandık — RESEND ETME. J1939-81 §5.4.4: sadece kaybeden
         * taraf yeni adres talep etmeli; kazanan tarafın claim retransmit
         * etmesi AC storm yaratır. Sadece logla (opsiyonel). */
        /* (intentionally no-op) */
    }
}

/* 1ms tick'te çağrılır (SysTick veya RTOS timer) */
void j1939_ac_tick(void)
{
    if (ac_state != AC_WAIT) return;
    if (++ac_timer_ms >= 250U) {
        ac_state = AC_CLAIMED;
        on_address_claimed(my_address);
    }
}

/* Başlangıçta çağrılır */
void j1939_start(void)
{
    /* Request PGN 0xEE00 — mevcut node'ları sorgula */
    uint8_t req[3] = { 0x00, 0xEE, 0x00 };
    uint32_t req_id = j1939_make_id(6, 0x00EA00, J1939_NULL_ADDR);
    fdcan_send_ext(req_id, req, 3);
    osDelay(250U);  /* cevapları topla — §5.4.5 */
    ac_send_claim(preferred_address);
}
```

**ISO J1939-81 kritik kurallar:**
- AC mesajı göndermeden ÖNCE SA=0xFE (Null) olmalı
- Çakışma kayıptan sonra 0-153ms rastgele bekleme şart (bus storm önleme)
- 250ms pencerede itiraz gelmezse adres kesinleşir
- Tüm adresler doluysa NULL adresle "cannot claim" gönder — sessiz kalma yasak

---

## CMDT Transport Protocol — Tam State Machine

CMDT peer-to-peer, akış kontrollü — ECU reprogramlama için kullanılır:

```c
typedef enum {
    CMDT_TX_IDLE, CMDT_TX_WAIT_CTS, CMDT_TX_SEND, CMDT_TX_WAIT_EOM
} cmdt_tx_state_t;

typedef struct {
    cmdt_tx_state_t state;
    uint8_t         da;
    const uint8_t  *payload;
    uint16_t        total_len;
    uint8_t         pkt_total;
    uint8_t         pkt_sent;
    uint8_t         cts_pkts;
    uint32_t        timer_ms;
} cmdt_tx_t;

static cmdt_tx_t cmdt_tx;

void cmdt_send_rts(uint8_t da, const uint8_t *data, uint16_t len, uint32_t pgn)
{
    cmdt_tx.da        = da;
    cmdt_tx.payload   = data;
    cmdt_tx.total_len = len;
    cmdt_tx.pkt_total = (uint8_t)((len + 6) / 7);
    cmdt_tx.pkt_sent  = 0;
    cmdt_tx.state     = CMDT_TX_WAIT_CTS;
    cmdt_tx.timer_ms  = 0;

    uint8_t rts[8] = {
        0x10,
        len & 0xFF, (len >> 8) & 0xFF,
        cmdt_tx.pkt_total,
        0xFF,   /* max pkts per CTS — receiver decides */
        pgn & 0xFF, (pgn >> 8) & 0xFF, (pgn >> 16) & 0xFF
    };
    fdcan_send_ext(j1939_make_id(7, 0x00EC00 | da, my_address), rts, 8);
}

/* CAN RX'te gelen TP.CM işleme */
void cmdt_tx_rx(const uint8_t *data)
{
    uint8_t ctrl = data[0];
    if (ctrl == 0x11) {                    /* CTS */
        cmdt_tx.cts_pkts = data[1];
        cmdt_tx.timer_ms = 0;
        cmdt_tx.state    = CMDT_TX_SEND;
        /* data[2] = next packet number to send */
        for (uint8_t i = 0; i < cmdt_tx.cts_pkts
             && cmdt_tx.pkt_sent < cmdt_tx.pkt_total; i++) {
            cmdt_tx.pkt_sent++;
            uint8_t dt[8];
            dt[0] = cmdt_tx.pkt_sent;
            uint16_t off = (cmdt_tx.pkt_sent - 1) * 7;
            for (int j = 0; j < 7; j++)
                dt[1 + j] = (off + j < cmdt_tx.total_len)
                           ? cmdt_tx.payload[off + j] : 0xFF;
            fdcan_send_ext(j1939_make_id(7, 0x00EB00 | cmdt_tx.da, my_address), dt, 8);
        }
        if (cmdt_tx.pkt_sent >= cmdt_tx.pkt_total)
            cmdt_tx.state = CMDT_TX_WAIT_EOM;

    } else if (ctrl == 0x13) {             /* EOM_ACK */
        cmdt_tx.state = CMDT_TX_IDLE;
        on_cmdt_complete(true);

    } else if (ctrl == 0xFF) {             /* Abort */
        cmdt_tx.state = CMDT_TX_IDLE;
        on_cmdt_complete(false);
    }
}

/* Timeout kontrolü — 1ms tick */
void cmdt_tx_tick(void)
{
    if (cmdt_tx.state == CMDT_TX_IDLE) return;
    cmdt_tx.timer_ms++;
    if (cmdt_tx.state == CMDT_TX_WAIT_CTS && cmdt_tx.timer_ms > 1250U) {
        uint8_t abort[8] = { 0xFF, 0xFE, 0x00, 0xFF, 0xFF, 0x00, 0x00, 0x00 };
        fdcan_send_ext(j1939_make_id(7, 0x00EC00 | cmdt_tx.da, my_address), abort, 8);
        cmdt_tx.state = CMDT_TX_IDLE;
        on_cmdt_complete(false);
    }
    if (cmdt_tx.state == CMDT_TX_WAIT_EOM && cmdt_tx.timer_ms > 1250U) {
        cmdt_tx.state = CMDT_TX_IDLE;
        on_cmdt_complete(false);
    }
}
```

**CMDT Timeout tablosu (J1939-21 Table B2):**
```
T1 = 750ms   — TP.DT frame aralığı max (alıcı perspektifi)
T2 = 1250ms  — CTS bekleme (gönderici)
T3 = 1250ms  — EOM_ACK bekleme (gönderici)
T4 = 1050ms  — CTS sonrası DT gönderme başlama (alıcı)
```

---

## STM32 FDCAN Bit Timing Hesabı (J1939 250kbps)

```
Formül:
  Bit Time = TSEG1 + TSEG2 + 1 (sync segment)
  TQ = FDCAN_clock / NominalPrescaler
  Bit rate = 1 / (TQ * (1 + TSEG1 + TSEG2))

Örnek: FDCAN_clock=80MHz, 250kbps, 16 TQ, sample point=87.5%:
  NominalPrescaler = 80_000_000 / (250_000 * 16) = 20
  TSEG1 = 13  → sample point = (1+13)/16 = 87.5%
  TSEG2 = 2
  SJW   = 1

Örnek: FDCAN_clock=120MHz, 250kbps:
  NominalPrescaler = 120_000_000 / (250_000 * 16) = 30
  TSEG1 = 13, TSEG2 = 2, SJW = 1
```

**KRITIK TUZAK:** STM32H7'de FDCAN saat kaynağı PLL1Q'dur, PCLK1 değil:
```c
RCC_PeriphCLKInitTypeDef prph = {0};
prph.PeriphClockSelection = RCC_PERIPHCLK_FDCAN;
prph.FdcanClockSelection  = RCC_FDCANCLKSOURCE_PLL;  /* PLL1Q */
HAL_RCCEx_PeriphCLKConfig(&prph);
/* PLL1Q frekansını doğrula: HAL_RCCEx_GetPeriphCLKFreq(RCC_PERIPHCLK_FDCAN) */
```
Prescaler hesabı FDCAN_CLK'a göre yapılmalı, SYSCLK veya PCLK değil.

---

## DM1 — Tam SPN/FMI Parsing

SPN 19-bit field, J1939 DTC içinde karmaşık biçimde paketlenir:

```c
typedef struct {
    uint32_t spn;       /* 19-bit */
    uint8_t  fmi;       /* 5-bit */
    uint8_t  occ_count;
} j1939_dtc_t;

void dm1_parse(const uint8_t *payload, uint16_t len,
               j1939_dtc_t *dtcs, uint8_t *count)
{
    /* Byte 0: Lamp status, Byte 1: Flash — DTC'ler byte 2'den başlar */
    *count = 0;
    if (len < 6) return;  /* minimum: 2 header + 4 DTC */

    for (uint16_t off = 2; off + 3 < len && *count < 8; off += 4) {
        j1939_dtc_t *d = &dtcs[(*count)++];
        /* J1939-73 §5.7.1 DM1 (Conversion Method 4 / CM=0, modern):
         *   byte0 = SPN[7:0]            (little-endian low byte first)
         *   byte1 = SPN[15:8]
         *   byte2 = SPN[18:16] in bits [7:5], FMI in bits [4:0]
         *   byte3 = CM(bit7) | OC(bits[6:0])
         * CM=0 (modern); CM=1 is legacy and uses different byte order. */
        d->spn = ((uint32_t)payload[off + 0])
               | ((uint32_t)payload[off + 1] << 8)
               | ((uint32_t)(payload[off + 2] >> 5) << 16);
        d->fmi       = payload[off + 2] & 0x1FU;
        d->occ_count = payload[off + 3] & 0x7FU;
    }
}
```

---

## DM11 / DM14 / DM15 / DM16 — Bellek Erişim ve Programlama

### DM11 — DTC Temizle (PGN 0x00FEC1)
```
İstek (broadcast, SA=kendi adres):
  DATA:  FF FF FF FF FF FF FF FF  (tümü 0xFF — tüm DTC'leri sil)
  Yanıt: yok (acknowledge edilmez)
  Sonuç: DM1 + DM2 listeleri temizlenir
```

### DM14 → DM15 → DM16 Flash Reprogramlama Akışı
```
1. DM14 (Tester→ECU, PGN 0x00DED0, CMDT ile):
   Byte 0: Bellek erişim uzunluğu (byte)
   Byte 1: Komut (0x01=Erase, 0x02=Read, 0x03=Write)
   Byte 2-5: Başlangıç adresi (LE)
   Byte 6-7: 0xFF

2. DM15 (ECU→Tester, PGN 0x00DEB0): Yanıt
   Byte 0: 0x00=Kabul, 0xFE=Meşgul, 0xFF=Ret
   Byte 2-5: Echo adres

3. DM16 (Tester→ECU, PGN 0x00DC00): Veri gönder
   Byte 0-7: 8 byte data

Akış: DM14(erase) → DM15(ack) → DM14(write) → [DM16 x N] → DM15(complete)
Büyük transferler için CMDT TP üzerinden gönderilir.
```

---

## Proprietary A/B PGN — Üretici Mesajları

```
Proprietary A  (PDU1):  PGN 0x00EF00  — peer-to-peer (DA belirli)
Proprietary B  (PDU2):  PGN 0x00FF00..0x00FFFF — broadcast

PDU1 (0xEF00), hedef DA=0x0A, kaynak SA=0x80:
  CAN_ID = 0x18EF0A80   [Pri=6, DP=0, PF=0xEF, PS=0x0A, SA=0x80]

PDU2 (0xFF01), kaynak SA=0x80:
  CAN_ID = 0x18FF0180   [Pri=6, DP=0, PF=0xFF, GE=0x01, SA=0x80]

Kullanım kuralları:
  - 0xEF00: Üretici komutları, kalibrasyon (P2P şifreli)
  - 0xFF00: Üretici broadcast (periyodik sensör, durum)
  - Proprietary PGN'ler diğer üreticilerle çakışabilir — DM14/15/16 gibi
    standart programlama servisleri daha güvenli alternatiftir
```

---

## J1939-82 Conformance Testing — Temel Gereksinimler

```
□ Address Claiming:
  - AC mesajı: PGN=0xEE00, priority=6, 29-bit EXT ID
  - NAME: 8 byte little-endian, tüm alanlar geçerli
  - Çakışma: 0-153ms rastgele gecikme + yeniden talep
  - Cannot Claim: SA=0xFE ile AC gönder, sessiz kalma yasak

□ Transport Protocol:
  - BAM inter-packet gap: 50-200ms
  - CMDT timeouts: T1=750ms, T2=1250ms, T3=1250ms, T4=1050ms
  - Max payload: 1785 byte

□ Message Timing (J1939-82 §A.2):
  - EEC1 (0xF004):  10ms ± 5%
  - CCVS (0xFEF1): 100ms ± 10%
  - Jitter toleransı: ±5ms

□ CAN Physical (J1939-82 §8):
  - 250 kbps: bit timing hata < 0.5%
  - Sample point: 75-87.5% tercih edilen
  - TQ: 16-25 (önerilen aralık)
  - 29-bit Extended ID zorunlu

□ Error Handling:
  - Bus-Off: uygulama kararı, otomatik kurtarma yasak
  - Auto-retransmit: ENABLE
  - Error frame logging şart (DM1/DM2 olarak)
```
