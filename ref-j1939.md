# SAE J1939 Protokol Referansı (STM32 / FDCAN)

SAE J1939: ağır araç (kamyon, otobüs, iş makinesi) CAN ağ standardı. 29-bit extended CAN ID, 8 byte veri, 250 kbps (bazı uyg. 500 kbps). ISO 11898-2 fiziksel katman.

---

## 29-bit CAN ID Yapısı

```
Bit[28:26] Priority         3 bit — 0 (en yüksek) … 7 (en düşük)
Bit[25]    Reserved (R)     1 bit — 0
Bit[24]    Data Page (DP)   1 bit — 0 veya 1 (PGN alanını genişletir)
Bit[23:16] PDU Format (PF)  8 bit — mesaj tipi belirler
Bit[15:8]  PDU Specific     8 bit — PF<0xF0: hedef adres; PF≥0xF0: grup uzantısı (GE)
Bit[7:0]   Source Address   8 bit — gönderen node adresi (0x00..0xFD)
                                    0xFE = Null (adres talep ediyor)
                                    0xFF = Broadcast (global adres)
```

**PGN (Parameter Group Number) hesabı:**
```
Eğer PF < 0xF0 (PDU1 — peer-to-peer):
    PGN = (DP << 17) | (PF << 8) | 0x00   ← PDU Specific hedef adres, PGN'e dahil değil

Eğer PF >= 0xF0 (PDU2 — broadcast):
    PGN = (DP << 17) | (PF << 8) | PS    ← PDU Specific grup uzantısı, PGN'e dahil
```

**Örnek CAN ID analizi:**
```
0x0CF00400 (hex) = 0000 1100 1111 0000 0000 0100 0000 0000 (binary)
  Priority = 3     (bits 28:26 = 011)
  R = 0, DP = 0
  PF = 0xF0        → PDU2 (broadcast)
  PS = 0x04        → Group Extension 4
  SA = 0x00        → Engine Control Module #1

PGN = 0x0000F004 = 61444 = EEC1 (Electronic Engine Controller 1)
```

---

## PDU1 vs PDU2

| | PDU1 | PDU2 |
|--|------|------|
| PF range | 0x00..0xEF | 0xF0..0xFF |
| PDU Specific | Hedef adres | Grup uzantısı (GE) |
| Yön | Peer-to-peer (DA belirli) | Broadcast (tüm node'lar) |
| PGN'de PS | Dahil değil | Dahil |
| Örnek | TP.CM (0xEC00), DT (0xEB00) | EEC1 (0xF004), CCVS (0xFEF1) |

---

## Yaygın PGN Tablosu

| PGN (hex) | Adı | Bit Rate | Period |
|-----------|-----|---------|--------|
| 0x00EE00 | Address Claimed | — | Event |
| 0x00EA00 | Request PGN | — | Event |
| 0x00EC00 | TP.CM (Transport Protocol Conn.Mgmt) | — | Event |
| 0x00EB00 | TP.DT (Transport Protocol Data Transfer) | — | Event |
| 0x00FE6B | Cab Illumination | 1 kbps | 1000ms |
| 0x00FEF1 | CCVS (Vehicle Speed) | — | 100ms |
| 0x00FEF2 | LFE (Fuel Economy) | — | 100ms |
| 0x00FEE5 | ET1 (Engine Temp) | — | 1000ms |
| 0x00FECA | DM1 (Active DTCs) | — | 1000ms |
| 0x00FECB | DM2 (Previously Active DTCs) | — | On Request |
| 0x00FEC1 | DM11 (Clear DTCs) | — | On Request |
| 0x00F001 | EEC3 | — | 250ms |
| 0x00F003 | EEC2 | — | 50ms |
| 0x00F004 | EEC1 (Engine Speed/Torque) | — | 10ms |
| 0x00F005 | ETC1 | — | 10ms |

---

## Address Claiming

Her J1939 node ağ katılımında adres talep eder.

```
Adres Talebi (Address Claimed — PGN 0x00EE00):
  CAN ID = [Pri=6, R=0, DP=0, PF=0xEE, PS=0xFF, SA=kendi_adres]
  DATA = 8 byte NAME (node kimliği, manufacturer code, vs)

Eğer başka node aynı adresi talep ederse:
  - Daha düşük NAME (numerik) kazanır
  - Kaybeden node farklı adres seçer veya Null adresle devam eder

Adres sorgusu (Request — PGN 0x00EA00):
  CAN ID = [Pri=6, R=0, DP=0, PF=0xEA, PS=0xFF, SA=0xFE]
  DATA = 00 EE 00 FF FF FF FF FF  (PGN 0xEE00 talep et, FF ile doldur)
```

**NAME yapısı (8 byte, LSB first):**
```
bits[63:60] Industry Group    4 bit (0=Global, 1=Otomasyon, 2=Agri, 3=Const, 4=Marine)
bits[59]    Vehicle System Instance 1 bit
bits[58:51] Vehicle System  8 bit
bits[50]    Reserved         1 bit
bits[49:42] Function         8 bit
bits[41:39] Function Instance 3 bit
bits[38:35] ECU Instance      4 bit
bits[34:11] Manufacturer Code 11 bit
bits[10:0]  Identity Number   21 bit
```

---

## Transport Protocol (TP) — Büyük Mesaj Transferi

J1939-TP: 9 ila 1785 bayt arası mesajlar için. İki mod:

### BAM (Broadcast Announce Message) — Broadcast

```
1. TP.CM_BAM gönder (PGN 0x00ECFF):
   Byte 0:   0x20 (BAM control byte)
   Byte 1-2: Mesaj boyutu (little-endian)
   Byte 3:   Paket sayısı
   Byte 4:   0xFF (reserved)
   Byte 5-7: PGN (little-endian, 3 byte)

2. TP.DT çerçeveleri gönder (PGN 0x00EBFF, 50ms aralıklarla):
   Byte 0:   Sıra numarası (1..255)
   Byte 1-7: Data (7 byte, son paket eksikse 0xFF ile doldur)
```

**Örnek — 20 byte BAM:**
```
TP.CM_BAM: 20 14 00 03 FF 18 F0 00   → 20byte, 3 paket, PGN=0x00F018
TP.DT:     01 D1 D2 D3 D4 D5 D6 D7  → paket 1
TP.DT:     02 D8 D9 DA DB DC DD DE  → paket 2
TP.DT:     03 DF FF FF FF FF FF FF  → paket 3 (son 1 byte + 6 byte pad)
```

### CMDT (Connection Mode Data Transfer) — Peer-to-Peer

```
1. TP.CM_RTS → Alıcıya RTS gönder (PGN 0x00EC + DA):
   Byte 0: 0x10 (RTS control byte)
   Byte 1-2: Mesaj boyutu
   Byte 3: Paket sayısı
   Byte 4: Max paket/CTS (0xFF=no limit)
   Byte 5-7: PGN

2. Alıcı TP.CM_CTS ile onaylar (Byte 0=0x11, byte 1=kabul edilen paket sayısı)

3. Gönderici TP.DT gönderir

4. Alıcı EOM onaylar (Byte 0=0x13)

Hata: 0xFF = Connection Abort (Byte 0=0xFF, Byte 1=hata kodu)
```

---

## DM1 — Aktif DTC Mesajı (PGN 0x00FECA)

```
Byte 0: Lamp status (MIL, Red Stop, Yellow, Protect)
Byte 1: Flash (reserved)
Byte 2-3: DTC (SPN bits[18:11], SPN bits[10:3])
Byte 4:   SPN bits[2:0] + FMI bits[4:0]
Byte 5:   Occurrence count (0..126, 127=unknown)
```

**SPN (Suspect Parameter Number):** Hangi sinyal/sensör  
**FMI (Failure Mode Indicator):** Nasıl arızalandı

| FMI | Açıklama |
|-----|---------|
| 0 | Üst limit aşıldı (>high) |
| 1 | Alt limit aşıldı (<low) |
| 2 | Veri hatalı |
| 3 | Voltaj yüksek / açık devre |
| 4 | Voltaj düşük / kısa devre |
| 5 | Akım düşük |
| 6 | Akım yüksek |
| 7 | Mekanik arıza |
| 8 | Anormal frekans |
| 9 | Anormal güncelleme hızı |
| 10 | Anormal değişim oranı |
| 11 | Kök neden bilinmiyor |
| 12 | Kötü akıllı cihaz |
| 13 | Out of calibration |
| 14 | Özel talimat |
| 31 | Koşul yok |

---

## STM32H7 FDCAN — J1939 Yapılandırması

```c
/* J1939: 29-bit extended ID, 250 kbps (veya 500 kbps) */
FDCAN_HandleTypeDef hfdcan1;

hfdcan1.Init.ClockDivider         = FDCAN_CLOCK_DIVIDER1;
hfdcan1.Init.FrameFormat          = FDCAN_FRAME_CLASSIC;   /* CAN 2.0B — J1939 klasik */
hfdcan1.Init.Mode                 = FDCAN_MODE_NORMAL;
hfdcan1.Init.AutoRetransmission   = ENABLE;
hfdcan1.Init.TransmitPause        = ENABLE;   /* J1939 bus access delay */
hfdcan1.Init.ProtocolException    = ENABLE;

/* 250 kbps @ 80 MHz FDCAN clock: prescaler=16, TSEG1=13, TSEG2=2, SJW=1 */
hfdcan1.Init.NominalPrescaler     = 16;
hfdcan1.Init.NominalSyncJumpWidth = 1;
hfdcan1.Init.NominalTimeSeg1      = 13;
hfdcan1.Init.NominalTimeSeg2      = 2;

/* J1939 global filtre: sadece EXT ID kabul et, STD ID reddet */
HAL_FDCAN_ConfigGlobalFilter(&hfdcan1,
    FDCAN_REJECT,           /* non-matching STD frames */
    FDCAN_ACCEPT_IN_RX_FIFO0, /* non-matching EXT frames → FIFO0 */
    FDCAN_FILTER_REMOTE,
    FDCAN_FILTER_REMOTE);

/* Filtre: belirli PGN range kabul — örn. EEC1 + FECA + TP çerçeveleri */
FDCAN_FilterTypeDef f;
f.IdType       = FDCAN_EXTENDED_ID;
f.FilterIndex  = 0;
f.FilterType   = FDCAN_FILTER_MASK;
f.FilterConfig = FDCAN_FILTER_TO_RXFIFO0;
/* PGN 0xF004 (EEC1) — herhangi kaynak adresten */
/* 29-bit mask: priority(3)+R(1)+DP(1)+PF(8)+PS(8)+SA(8) */
/* Sadece PF+PS maskele (PGN kısmı), SA=don't care */
f.FilterID1    = 0x0CF00400;  /* EEC1, SA=0x00 */
f.FilterID2    = 0x03FFFF00;  /* mask: PGN alanını maskele, SA don't care */
HAL_FDCAN_ConfigFilter(&hfdcan1, &f);
```

**29-bit J1939 CAN ID inşası:**
```c
uint32_t j1939_make_id(uint8_t priority, uint32_t pgn, uint8_t src_addr)
{
    /* PDU2: PF >= 0xF0 → PS PGN'in içinde */
    uint8_t pf = (pgn >> 8) & 0xFF;
    uint8_t ps = pgn & 0xFF;
    uint8_t dp = (pgn >> 16) & 0x01;

    return ((uint32_t)(priority & 0x07) << 26)
         | ((uint32_t)(dp)              << 24)
         | ((uint32_t)(pf)              << 16)
         | ((uint32_t)(ps)              <<  8)
         | ((uint32_t)(src_addr));
}

/* EEC1 gönder */
uint32_t can_id = j1939_make_id(3, 0x00F004, MY_SA);
FDCAN_TxHeaderTypeDef hdr = {
    .Identifier          = can_id,
    .IdType              = FDCAN_EXTENDED_ID,
    .TxFrameType         = FDCAN_DATA_FRAME,
    .DataLength          = FDCAN_DLC_BYTES_8,
    .BitRateSwitch       = FDCAN_BRS_OFF,
    .FDFormat            = FDCAN_CLASSIC_CAN,
    .TxEventFifoControl  = FDCAN_NO_TX_EVENTS,
    .MessageMarker       = 0,
};
```

---

## J1939 Adres Yönetimi — STM32 Uygulama

```c
#define J1939_NULL_ADDR    0xFE
#define J1939_GLOBAL_ADDR  0xFF
#define J1939_INITIAL_ADDR 0x80   /* varsayılan talep adresi */

static uint8_t my_address = J1939_NULL_ADDR;
static uint64_t my_name;

void j1939_claim_address(uint8_t desired_addr)
{
    my_address = desired_addr;
    /* Address Claimed mesajı gönder */
    uint32_t can_id = j1939_make_id(6, 0x00EE00, desired_addr);
    uint8_t name_bytes[8];
    memcpy(name_bytes, &my_name, 8);  /* little-endian NAME */
    fdcan_send(can_id, name_bytes, 8);
}

/* Gelen Address Claimed'i kontrol et — çakışma var mı? */
void j1939_rx_address_claimed(uint32_t can_id, const uint8_t *data)
{
    uint8_t sender_sa  = can_id & 0xFF;
    uint64_t sender_name;
    memcpy(&sender_name, data, 8);

    if (sender_sa == my_address) {
        if (sender_name < my_name) {
            /* Kaybettik — yeni adres seç veya null address kullan */
            my_address++;   /* basit: bir sonraki adres dene */
            if (my_address >= 0xFE) my_address = 0x80;
            osDelay(osKernelGetTickCount() % 153);  /* rastgele gecikme (J1939 gereksinimi) */
            j1939_claim_address(my_address);
        } else {
            /* Kazandık — tekrar claim et */
            j1939_claim_address(my_address);
        }
    }
}
```

---

## TP BAM Gönderim — STM32 Uygulama İskeleti

```c
void j1939_send_bam(uint32_t pgn, const uint8_t *data, uint16_t len)
{
    uint8_t num_packets = (uint8_t)((len + 6) / 7);

    /* 1. TP.CM_BAM */
    uint8_t cm[8] = {
        0x20,                      /* BAM */
        len & 0xFF, (len >> 8) & 0xFF,
        num_packets,
        0xFF,
        pgn & 0xFF, (pgn >> 8) & 0xFF, (pgn >> 16) & 0xFF
    };
    fdcan_send(j1939_make_id(7, 0x00ECFF, my_address), cm, 8);

    /* 2. TP.DT — 50ms aralıklarla */
    for (uint8_t pkt = 1; pkt <= num_packets; pkt++) {
        osDelay(50U);
        uint8_t dt[8];
        dt[0] = pkt;
        uint16_t offset = (pkt - 1) * 7;
        for (int i = 0; i < 7; i++) {
            dt[1 + i] = (offset + i < len) ? data[offset + i] : 0xFF;
        }
        fdcan_send(j1939_make_id(7, 0x00EBFF, my_address), dt, 8);
    }
}
```

---

## Yaygın J1939 Hataları

| Hata | Neden | Çözüm |
|------|-------|-------|
| Mesaj alınamıyor | STD ID filtre aktif | `FDCAN_ACCEPT_IN_RX_FIFO0` EXT için |
| PGN yanlış hesaplanıyor | PDU1/PDU2 ayrımı | PF<0xF0: PS adres, PGN'de yok |
| TP timeout | BAM aralarında 50ms yok | `osDelay(50U)` zorunlu |
| Adres çakışması çözülmüyor | NAME karşılaştırması yanlış | NAME little-endian, uint64_t karşılaştır |
| DM1 parse hatası | SPN bit order yanlış | SPN=byte[2-4] karmaşık packing |
| Bus-off | 250 kbps yanlış | prescaler/TSEG kristal frekansına göre hesapla |
| 29-bit ID oluşturulamıyor | STD ID mode seçili | `FDCAN_EXTENDED_ID` kullan |
